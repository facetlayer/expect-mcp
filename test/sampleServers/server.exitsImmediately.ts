#!/usr/bin/env node

/**
 * Sample MCP server that exits immediately with a non-zero code.
 * Used for testing error handling when a subprocess fails to start.
 */

// Exit immediately with non-zero code
process.exit(42);
