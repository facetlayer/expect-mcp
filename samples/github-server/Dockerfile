# Use the official GitHub MCP server image
FROM ghcr.io/github/github-mcp-server:latest

# Note: This server requires a GITHUB_PERSONAL_ACCESS_TOKEN environment variable
# The token should be passed when running the container
